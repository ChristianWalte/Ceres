{% set itemsData = config("Walcer.basket.item_data")|split(', ') %}
{% set showItemVariation = config("Walcer.basket.variations") %}
{% set configItemName = config("Walcer.item.name") %}

<script type="x/template" id="vue-basket-list-item">

    <div v-if="size == 'large'">
        {% include "Walcer::Basket.Components.BasketListItem_large" %}
    </div>

    <div v-if="size == 'small'">
        {% include "Walcer::Basket.Components.BasketListItem_small" %}
    </div>

</script>
